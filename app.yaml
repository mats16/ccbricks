command: ["npm", "run", "start", "--workspace=apps/api"]

env:
  - name: USER_BASE_DIR
    value: /home/app/users
  - name: ENCRYPTION_KEY
    valueFrom: encryption-key-secret
  - name: DATABASE_URL
    valueFrom: database-url-secret
