resources:
  apps:
    lakepixie_app:
      name: ${var.app_name}
      description: Log exploration on Databricks
      source_code_path: ../
      #resources:
      #  - name: database-url-secret
      #    description: Database connection string
      #    secret:
      #      scope: lakepixie-${bundle.target}
      #      key: database-url
      #      permission: READ
      user_api_scopes:
        - sql
        - serving.serving-endpoints
        - files.files
        - dashboards.genie
        - catalog.catalogs:read
        - catalog.schemas:read
        - catalog.tables:read
        - mcp.external
        - mcp.genie
        - mcp.functions
        - mcp.vectorsearch
        - apps
        - workspace.workspace
      permissions:
        - level: CAN_USE
          group_name: users
